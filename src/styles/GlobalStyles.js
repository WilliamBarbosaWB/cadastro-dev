import { createGlobalStyle } from "styled-components";

export const GlobalStyles = createGlobalStyle`

    *{ //colocando os estilos globais na aplicação toda.
    margin:0;
    padding: 0;
    box-sizing:border-box;
    font-family: "Roboto", sans-serif;

}

`
